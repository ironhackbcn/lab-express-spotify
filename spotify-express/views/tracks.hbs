<div class="tracks-title">
  <h2>Tittle</h2>
  <h2>Listen</h2>
</div>
<div class="tracks">
  {{#each tracks}}
  <div class="inside-tracks">
    <p class="p-track">{{this.name}}</p>
    <audio controls>
      <source src="{{this.preview_url}}">
    </audio>
  </div>
  {{/each}}
</div>