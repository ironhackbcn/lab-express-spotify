<h1>Albums for {{artistAlbums.artist}}</h1>
  <div class="container">
    {{#each artistAlbums.albums}}
    <div>
      <img src="{{images.0.url}}" alt="">
      <h2>{{name}}</h2>
      <a href="/tracks/{{id}}">View tracks</a>
    </div>
    {{/each}}
  </div>