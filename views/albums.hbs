<h1>{{this.name}}</h1>

<ul>
  {{#each albums}}    
      <div>
        <img src="{{this.images.0.url}}" alt="" width="100px" height="100px">
        <p>{{this.name}}</p>
        <button src=""><a href="/tracks/{{this.id}}">View Tracks</a></button>
      </div>
  {{/each}}
</ul>