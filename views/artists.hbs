
<section>
{{#each body.artists.items}} 
<li>
    <article>
        <img src={{images.0.url}} >
        <p>{{name}}</p>
        <a href="/albums/{{id}}">View albums</a>
    </article>
</li>
{{/each}}
</section>