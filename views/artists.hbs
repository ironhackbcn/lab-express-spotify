<div>
  <div>
    {{#each artist}}
      <h2>{{name}}</h2>
      {{#if images}}
        <img src="{{images.0.url}}" alt="{{name}}">
      {{else}}
        <img src="" alt="Default image">
      {{/if}}
    <button><a href="/albums/{{id}}">View Albums</a></button>
    {{/each}}
  </div>
</div>