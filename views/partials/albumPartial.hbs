<div class="album-card">
  {{#if images}}
  <img src="{{images.0.url}}" alt="Cover of {{name}}">
  {{else}}
  <img src="/images/default-artist.png" alt="Placeholder album picture">
  {{/if}}
  <h2>{{name}}</h2>
  <button onclick="window.location.href='/tracks/{{id}}'">View Tracks</button>
</div>