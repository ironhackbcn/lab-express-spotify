<div class="cards-container track">
  {{#each songs}}
    <div class="track-container">
      {{name}}
      <audio
            controls
            src="{{preview_url}}">
                Your browser does not support the
                <code>audio</code> element.
      </audio>
    </div>
  {{/each}}
</div>