<h1>LIST OF TRACKS</h1>
<H3>TITLE</H3>
<H3>LISTEN</H3>
<div class="container">
    <image src="{{this.image.0.url}}"></image>"
    {{#each items}}
    {{this.name}}

    <audio controls src="{{this.preview_url}}">Your browser does not support the
        <code>audio</code>element.
    </audio>

    {{/each}}
</div>